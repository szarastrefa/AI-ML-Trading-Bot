# AI/ML Trading Bot v3.1.1 - SIMPLIFIED COMPATIBILITY
# TensorFlow 2.16.1 with built-in Keras (no tf-keras dependency)
# Module import error FIXED

# =============================================================================
# WEB FRAMEWORK - COMPATIBLE VERSIONS
# =============================================================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
jinja2==3.1.2
python-dotenv==1.0.0

# =============================================================================
# DATA PROCESSING - COMPATIBLE
# =============================================================================
numpy==1.26.4               # Latest stable, compatible with TF 2.16+
pandas==2.0.3               # Stable version
scipy==1.11.4               # Compatible with numpy 1.26.4

# =============================================================================
# MACHINE LEARNING - SIMPLIFIED STACK  
# =============================================================================
scikit-learn==1.3.2         # Latest compatible
joblib==1.3.2               # Stable version

# TensorFlow - STANDALONE (includes Keras built-in)
tensorflow==2.16.1          # COMPATIBLE with typing-extensions >=4.6.1
# Note: Keras is included with TensorFlow 2.16.1 - no separate install needed
# Use TF_USE_LEGACY_KERAS=1 environment variable for Keras 2.x API

# =============================================================================
# FINANCIAL DATA - ESSENTIAL
# =============================================================================
yfinance==0.2.28            # Market data
ccxt==4.1.28                # Multi-exchange support  
requests==2.31.0            # HTTP requests

# =============================================================================
# DATABASE - REQUIRED
# =============================================================================
sqlalchemy==2.0.23          # Database ORM

# =============================================================================
# VISUALIZATION - WEB GUI
# =============================================================================
plotly==5.17.0              # Interactive charts

# =============================================================================
# UTILITIES - MINIMAL SET
# =============================================================================
python-dateutil==2.8.2     # Date handling
pytz==2023.3                # Timezone support

# =============================================================================
# COMPATIBILITY NOTES:
# =============================================================================
# ✅ TensorFlow 2.16.1 includes Keras - no separate install needed
# ✅ Use TF_USE_LEGACY_KERAS=1 environment variable for Keras 2.x API
# ✅ All packages compatible with typing-extensions >=4.8.0
# ✅ Module import fixed: uvicorn.run("main:app") instead of "app.main:app"
# ✅ Simplified dependencies for maximum Docker build stability
# =============================================================================